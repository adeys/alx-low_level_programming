#include <stdio.h>

/**
 * main - Entry point
 * Prints a string using "printf"
 *
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
